# 群聊日报插件 (KiraDaily)

> 基于 KiraAI 框架的图片式智能群聊日报生成插件 —— 让 AI 自动为你总结群聊精华和吐槽吧

**版本：v1.1.0**

---

## ✨ 效果展示

<div align="center">

| 暖黄手账风 | 智能分析 | 活跃用户 |
|:---:|:---:|:---:|
| ![日报示例](demo.jpg) | 自动提取话题 + 金句 | 用户头像 + 活跃度排名 |

</div>

---

## 🎯 功能特色

### 智能分析
- **📊 消息统计**：总消息数、参与人数、最活跃时段
- **💬 话题提取**：LLM 自动提取群聊热门话题（带 AI 吐槽）
- **👤 活跃用户**：识别发言最多的用户及发言数
- **💡 金句识别**：自动筛选群聊中的精彩发言，并评选"最佳金句"
- **🤖 AI 锐评**：带人设风格的毒舌/温柔群聊点评

### 可视化报告
- **🖼️ 精美图片**：暖黄手账风 HTML 渲染图片，支持头像展示
- **📄 纯文本模式**：省 Token 但同样清晰的文字报告
- **👥 用户头像**：自动获取并展示群成员头像（带缓存）
- **🎨 随机配色**：每个活跃用户名称拥有不同颜色

### 灵活配置
- **⏰ 定时任务**：每天指定时间自动生成日报（默认 23:59）
- **📋 手动触发**：`/日报` 命令或自然语言调用
- **🔄 增量分析**：滑动窗口分批处理，日均消息 > 500 条的群也能轻松应对
- **👮 权限控制**：冷却时间 + 白名单用户豁免
- **🧑‍🎨 人设注入**：使用 KiraAI 当前人设风格进行锐评
- **🚫 发送者屏蔽**：可配置忽略特定发送者（如 system 消息）

### 技术亮点
- **🔍 自动检测浏览器**：优先使用系统 Chrome/Edge，无则自动下载内置 Chromium
- **💾 消息持久化**：SQLite 存储消息，支持高效查询
- **🔄 Token 优化**：消息截断 + 增量分析，大幅节省 Token
- **🎨 精美模板**：暖黄手账风 HTML，随机边框/配色，支持群头像印章效果

---

## 📦 安装

### 方式一：WebUI 一键安装（推荐）

1. 打开 KiraAI WebUI → 插件管理
2. 点击 "Install from GitHub"
3. 输入：`https://github.com/znq19/kira_daily_report`
4. 点击安装，启用插件即可

> 💡 **无需手动安装浏览器**：插件会在首次运行时自动检测系统中的 Chrome/Edge/Chromium；若没有，则自动下载 Playwright 内置 Chromium（仅需一次，约 150MB），理论上**全程异步，不会阻塞 KiraAI 启动**。

### 方式二：手动放置

```bash
# 1. 克隆到插件目录
cd KiraAI/data/plugins/
git clone https://github.com/znq19/kira_daily_report

# 2. 安装依赖
cd kira_daily_report
pip install -r requirements.txt

# 3. 重启 KiraAI
```

> 💡 **浏览器自动处理**：插件启动后会自动检测系统浏览器（Chrome/Edge/Chromium），若均不存在则自动下载内置 Chromium。整个过程无需手动干预。

---

## ⚙️ 配置说明

在 KiraAI WebUI 中找到本插件，以下配置项均可按需调整：

### 基础设置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 可使用日报的群组 | 空 | 留空=所有群；填入会话ID(如 `qq:gm:123456`)则仅这些群生效 |
| 定时开启日报的群组 | 空 | 留空=与可使用群组一致；填入则仅这些群自动生成 |
| 启用命令触发 | ❌ | 开启后 `/日报` 命令有效（默认关闭） |
| 启用自然语言触发 | ✅ | 关闭后 AI 不会自动调用工具 |
| 触发命令列表 | `["/日报", "/群日报"]` | 自定义触发命令 |

### 分析参数

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 分析天数 | 1 天 | 日报分析最近几天的消息 |
| 单次最大分析消息数 | 500 条 | 超过此数量将截断（防止 Token 爆炸） |
| 最小消息数阈值 | 10 条 | 少于该值不生成日报（省 Token） |
| 自动分析时间 | `23:59` | 每天自动生成日报的时间 |
| 启用自动日报 | ✅ | 关闭后定时任务不运行 |

### 增量分析模式（beta）

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 启用增量分析模式 | ❌ | 开启后分批次处理大消息群 |
| 增量分析群列表 | 空 | 哪些群走增量模式，留空=全部 |
| 增量分析间隔 | 120 分钟 | 每隔多少分钟执行一次小批量分析 |
| 增量最小消息数 | 10 条 | 少于该值不触发增量分析 |
| 滑动窗口大小 | 24 小时 | 生成日报时合并最近多少小时的数据 |

> 💡 **增量模式是什么？** 群消息量巨大的群（日均 > 500 条），一次性分析会消耗大量 Token。增量模式将消息分批处理，每次只分析新增消息，最后合并成完整日报 —— **大幅节省 Token，且不丢失上下文**。

### 内容设置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 最少话题数 | 1 | 不足则显示 `？` |
| 最多话题数 | 5 | 最多展示的话题数 |
| 金句数量 | 3 | 展示的金句数（0=不展示） |
| 活跃用户数量 | 10 | 展示的活跃用户数 |
| 启用 AI 锐评 | ✅ | 日报末尾增加人设风格锐评 |
| 注入人设风格 | ✅ | 使用 KiraAI 当前人设进行点评 |
| 指定人设 ID | `default` | 留空=使用当前活跃人设 |
| 屏蔽的发送者 | `["system"]` | 这些发送者的消息不会被收集 |

### 权限与安全

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 调用冷却时间 | 4 小时 | 同一群两次日报的最小间隔 |
| 白名单用户 | 空 | 填入 QQ 号，每行一个，不受冷却限制 |
| 白名单用户豁免冷却 | ✅ | 关闭后白名单用户也受冷却限制 |

### 输出与渲染

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 输出格式 | `image` | `image`=图片，`text`=纯文本 |
| 优先使用系统浏览器 | ✅ | 开启后优先使用系统 Chrome/Edge；关闭则强制使用内置 Chromium |
| 渲染超时时间 | 120 秒 | HTML 渲染为图片的超时时间，超时后任务失败 |
| 头像缓存过期天数 | 3 天 | 0=永不过期 |
| 报告保留天数 | 7 天 | 超期报告自动清理 |
| 每次清理报告数量 | 7 个 | 保留 N+1 个，其余删除 |
| 详细日志 | ❌ | 开启后输出调试日志 |

---

## 🚀 使用方法

### 方式一：斜杠命令（需开启 `enable_command_trigger`）

在群聊中发送：
```
/日报
```
或
```
/群日报
```

机器人会立即生成并发送日报图片。

### 方式二：自然语言（需开启 `enable_natural_language_trigger`）

在群聊中对 AI 说：
```
总结一下今天的群聊
生成日报
看看群里今天都聊了啥
```

AI 会自动调用 `generate_daily_report` 工具生成日报。

### 方式三：定时自动生成

配置好 `自动分析时间`（如 `23:59`），插件会在每天该时间自动为已启用的群生成日报。

---

## 🖼️ 日报包含什么？

| 模块 | 说明 |
|------|------|
| 📖 群聊日报 | 群头像印章 + 群名称 + 日期 |
| 💬 消息总数 | `总消息数/上限`（如 `47/500` 表示截断） |
| 👥 参与人数 | 当天发言的不同用户数 |
| ⏰ 最活跃时段 | 如 `20:00-22:00` |
| 🔥 热门话题 | LLM 提取的 1-5 个话题 + AI 吐槽 |
| 👤 活跃用户 | 发言最多的 N 位用户 + 头像 + 发言数 |
| 💡 今日金句 | 精选发言 + 最佳金句 + AI 选取理由 |
| 🤖 AI 锐评 | 带人设风格的一句点评 |

---

## ❓ 常见问题

### 1. 日报不生成 / 显示"消息不足"

- 检查群是否在 `可使用日报的群组` 中（留空表示全部）
- 检查消息数是否 ≥ `最小消息数阈值`（默认 10 条）
- 检查是否在冷却中（默认 4 小时）
- 检查 `启用命令触发` 是否开启（如果用命令触发）

### 2. 图片不显示 / 渲染超时

- 插件会自动检测系统浏览器（Chrome/Edge），无需手动安装
- 如果系统无浏览器，插件会自动下载内置 Chromium（约 150MB）
- **推荐方案**：在插件配置中调大 `渲染超时时间`（默认 120 秒），同时需要在 KiraAI 系统配置中调大 `工具调用超时时间`（`bot_config.agent.tool_call_timeout`），**两者需保持同步，且系统超时 ≥ 插件渲染超时**，否则系统会先于插件超时中断任务。系统配置可在 KiraAI WebUI 的「系统配置 → Agent 设置」中修改，或直接编辑 `data/config/kira.json`：
  ```json
  {
    "bot_config": {
      "agent": {
        "tool_call_timeout": 180
      }
    }
  }
  ```
- 检查 `data/plugins/kira_daily_report/reports/` 目录是否有截图生成
- 尝试切换输出格式为 `text` 测试（排除渲染问题）

### 3. AI 锐评显示 "AI" 而非机器人昵称

- 在配置中填写 `指定机器人昵称`（如：小琪）
- 或确保 KiraAI 的 QQ 适配器已正确配置 `self_id`

### 4. 想要更多话题 / 金句

- 调高 `最多话题数` 或 `金句数量` 配置
- 注意：数量越多，Token 消耗越大

### 5. 不想让某些用户的消息被收集

- 在 `屏蔽的发送者` 中添加该用户的昵称（如 `system`、`小助手`，如果有群友自己改成这种想要欺骗bot的名字那就自己别想上榜咯！）
- 支持多个，每行一个

### 6. 增量模式不生效

- 确保 `启用增量分析模式` 为 ✅
- 确保群在 `增量分析群列表` 中（留空=全部）
- 首次运行需要等待一个 `增量分析间隔` 周期

### 7. 浏览器下载失败 / 渲染超时

- 确保网络可访问 `playwright.azureedge.net`
- 可手动安装浏览器：`playwright install chromium`
- 如果服务器资源紧张（内存 < 512MB），建议使用 `text` 输出格式
- 可尝试关闭 `优先使用系统浏览器`，强制使用内置 Chromium

### 8. 群头像未显示

- 检查群是否在 QQ 适配器中存在
- 头像缓存默认 3 天过期，过期后自动重新获取

### 9. `启用命令触发` 默认关闭，怎么开启？

在 WebUI 配置页面中，将 `启用命令触发` 开关打开即可。此设计是为了避免和其他框架误触命令，Kira精神指导下推荐使用自然语言触发方式。

### 10. `单次最大分析消息数` 在增量模式下怎么工作？

无论是否开启增量模式，该值都是**单次 LLM 调用的安全上限**。增量模式下，如果单次拉取的新消息超过该值，插件会自动截断为最新的 N 条，**剩余消息会在下次增量分析中继续处理**，不会丢失。

---

## 📁 文件结构

```
kira_daily_report/
├── main.py              # 插件主逻辑
├── manifest.json        # 插件元信息
├── schema.json          # 配置项定义
├── requirements.txt     # Python 依赖
├── utils/
│   ├── db.py            # SQLite 消息存储
│   ├── render.py        # Playwright HTML 渲染（自动检测浏览器）
│   └── avatar.py        # 头像下载与缓存
└── templates/
    └── daily_report.html # 日报 HTML 模板（暖黄手账风）
```

---

## 📝 更新日志

### v1.1.0 (2026-06-26)

**Bot 消息处理优化**
- 🔧 为防止重名引起统计和分析混乱，现在会搜集用户的`use_id`进行去重和唯一识别
- 🔧 Bot 自身消息现为内部固定行为：**收集但不参与 LLM 分析**（话题、金句、活跃用户均不包含 Bot），避免自循环污染
- 🔧 Bot 消息仍计入消息总数和参与人数（统计层面保留）
- 🔧 移除了 `collect_bot_messages`、`count_bot_in_stats`、`include_bot_in_llm_analysis` 配置项（保留内部接口以备后续开发）
- 🧹 精简调试日志，减少控制台刷屏

**文档优化**
- 📝 更新 FAQ，补充「渲染超时」需同时调整 KiraAI 系统全局 `tool_call_timeout` 的说明

### v1.0.0 (2026-06-26) — 首次发布 🎉

**核心功能**
- ✨ 群聊日报生成（图片/文本双模式）
- ✨ 消息自动收集与 SQLite 存储
- ✨ LLM 智能分析（话题提取、金句识别、活跃用户统计）
- ✨ AI 锐评 + 人设风格注入
- ✨ 暖黄手账风 HTML 模板（随机配色、头像展示）

**触发方式**
- ✨ `/日报` 斜杠命令（默认关闭，可配置开启）
- ✨ 自然语言调用（LLM 工具，默认开启）
- ✨ 定时自动生成（可配置时间）

**优化特性**
- ✨ 增量分析模式（滑动窗口分批处理，节省 Token）
- ✨ 消息截断 + 统计起止时间
- ✨ 自动冷却 + 白名单豁免
- ✨ 发送者屏蔽（`exclude_senders`）
- ✨ 头像缓存（自动过期刷新）
- ✨ 可配置渲染超时（默认 120 秒）
- ✨ 可配置是否优先使用系统浏览器（默认开启）

**技术实现**
- ✨ 自动检测系统浏览器（Chrome/Edge），无则自动下载 Chromium（异步不阻塞启动）
- ✨ 群头像印章从外边框突出
- ✨ 活跃用户名称随机纯色（12 种）
- ✨ AI 锐评显示真实机器人昵称
- ✨ 最佳金句高亮 + AI 选取理由
- ✨ 金句发送者头像作为背景（透明渐隐）

---

## 🙏 致谢

本插件参考/使用了以下优秀项目的设计思路和代码：

### [AstrBot 群聊日常分析插件](https://github.com/SXP-Simon/astrbot_plugin_qq_group_daily_analysis)
- 增量分析模式（滑动窗口）设计思想
- DDD 分层架构参考
- 定时任务调度设计
- 感谢 @SXP-Simon 的卓越工作

### [AIHTML 插件](https://github.com/OldTangyuan/KiraAI_ai_html)
- Playwright 浏览器自动检测与安装逻辑
- HTML 渲染为图片的核心实现
- 感谢 @OldTangyuan 的贡献

### KiraAI 框架
- 插件系统架构
- 适配器管理系统
- LLM 调用接口
- 人设管理系统

---

## 📄 许可证

AGPL-3.0 License

---

## 💬 交流反馈

- 📧 Issue （不如加入官群交流直接↓）
- 💬 KIraAI官方QQ 群: 874381335

欢迎提交 Issue 和 Pull Request 来改进这个插件！
```

---
